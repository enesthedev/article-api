generator client {
  provider = "prismaClientProvider-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Article {
  id      Int     @id @default(autoincrement())
  title   String  @unique
  content String
  active  Boolean @default(false)
}
